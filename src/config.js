import {Random} from "./js/my-code/random";

const config = {
  author: "Maksims Strašinskis",
  title: "Emojional Storm",
  instructions: "Random storm generation function - storm-generator.js. Use seed param to change randomizer results (new Random(seed)). Use \"magic numbers\" variables to change the storm options (sizes, angle, etc)"
};

export default config;
